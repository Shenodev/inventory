# The web app needs NO environment variables on Vercel: a production build
# falls back to https://api.inventory.shenodev.tech/api automatically.
# Only set this to point a build at a different API (e.g. local development).
VITE_API_BASE_URL=http://localhost:8000/api
